# deps / build
node_modules/
dist/

# secrets — never commit
.env
.env.*
!.env.example

# OS noise
.DS_Store

# playwright-mcp session artifacts (inspection scratch)
.playwright-mcp/

# internal project management + session tooling (local only)
CLAUDE.md
.claude/
.vercel
.env*
